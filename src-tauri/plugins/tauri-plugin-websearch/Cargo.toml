[package]
name = "tauri-plugin-websearch"
version = "0.8.3"
authors = ["Jan <service@jan.ai>"]
description = "Tauri plugin providing native, provider-neutral web_search / web_fetch tools (Exa backend by default)"
license = "MIT"
repository = "https://github.com/janhq/jan"
edition = "2021"
rust-version = "1.77.2"
exclude = ["/examples", "/dist-js", "/guest-js", "/node_modules"]
links = "tauri-plugin-websearch"

[features]
default = ["tauri"]
# The Tauri command/plugin surface. Disabled by the headless `jan` CLI, which
# consumes `provider` directly and must not link any GUI crate.
tauri = ["dep:tauri"]

[dependencies]
async-trait = "0.1"
jan-utils = { path = "../../utils" }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tauri = { version = "2.5.0", default-features = false, features = [], optional = true }
thiserror = "2.0.12"
tokio = { version = "1", features = ["full"] }
# `socks` enables socks4/socks5 proxy URLs (configured or via *_PROXY env);
# `system-proxy` lets reqwest read the OS-level proxy (macOS System
# Configuration, Windows registry) in addition to *_PROXY env vars — both are
# required for Web Search to honor proxies users already configured
# (janhq/jan#8951).
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "socks", "system-proxy"] }

[build-dependencies]
tauri-plugin = { version = "2.3.1", features = ["build"] }
